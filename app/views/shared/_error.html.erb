<div>
  <% if current_object.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(current_object.errors.count, "error") %> prohibited this <%= current_object.class.name %> from being saved:</h2>

      <ul>
        <% current_object.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
</div>

